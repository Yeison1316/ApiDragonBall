CREATE TABLE PERSONAJE (
	ID_PERSONAJE SERIAL,
	NOMBRE VARCHAR (100),
	DETALLE VARCHAR(4000),
	PODER VARCHAR(1000),
	CONSTRAINT PK_ID_PERSONAJE PRIMARY KEY (ID_PERSONAJE)
);

CREATE TABLE PLANETA (
	ID_PLANETA SERIAL,
	NOMBRE VARCHAR (100),
	DETALLE VARCHAR(4000),
	CONSTRAINT PK_ID_PLANETA PRIMARY KEY (ID_PLANETA)
);


INSERT INTO PERSONAJE
(NOMBRE,DETALLE,PODER)
VALUES
('Goku', 'Personaje de DBO',100)


npm install --save sequalize
npm install --save pg pg-hstore